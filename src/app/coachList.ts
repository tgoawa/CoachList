import { DataSource } from '@angular/cdk/collections';
import { Observable } from 'rxjs/Observable';

export class CoachList extends DataSource<any> {
  // connect(): Observable<TeamMemberList[]> {
  //   return Observable.of(data);
  // }

  // disconnect() { }
}
